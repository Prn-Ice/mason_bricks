name: auto_route_observer
description: Dart package that logs auto_route navigation events using the loggy package.
version: 1.1.0-dev.9
publish_to: none

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  auto_route: ^4.0.1
  # analyzer
  dart_code_metrics: ^4.16.0
  logger: ^1.1.0

dev_dependencies:
  coverage: ^1.5.0
  mocktail: ^0.3.0
  test: ^1.21.4
  very_good_analysis: "^3.0.1"
